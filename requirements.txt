torch>=2.1
tokenizers>=0.15
numpy>=1.24
datasets>=2.16    # only needed for the GPU run (TinyStories / FineWeb-Edu)
lm-eval>=0.4.13   # only needed for eval/run_harness_eval.py (HellaSwag/ARC-Easy/PIQA/WinoGrande/WikiText-103)
